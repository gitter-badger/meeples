.row-fluid
  %table.table.table-striped

    %thead
      %tr
        %th Email
        %th

    %tbody
      - @friends.each do |friend|
        %tr
          %td= link_to friend.email, user_path(friend)
